## Working todo list

For multi-step work, maintain a todo list with the `todo` tool: write the full list up front (action "write" replaces the entire list), keep exactly one item in_progress while you work on it, and mark items completed as soon as they are done. Skip the list for trivial single-step tasks.
